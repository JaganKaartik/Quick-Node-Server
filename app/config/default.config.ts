require('dotenv').config()

const {
  PORT,
  MONGO_URL,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  CLIENT_ORIGIN,
  JWT_SECRET
} = process.env

export {
  PORT,
  MONGO_URL,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  CLIENT_ORIGIN,
  JWT_SECRET
}
